# NGA狼大投资助手 - 工程转移指南

## 📋 工程概述

这是一个基于 NGA 狼大（ID: 150058）发言的智能投资分析助手，集成了以下功能：

### 核心功能
1. **智能爬取**：增量爬取狼大在指定帖子（TID: 45974302）的最新发言
2. **知识库管理**：对发言进行智能分析、标签分类、存储检索
3. **持仓管理**：支持自然语言记录买卖操作，实时计算盈亏
4. **盘中监测**：每10分钟自动爬取分析新发言
5. **每日/每周复盘**：23:59自动生成复盘报告，包含操作评分和市场分析
6. **智能对话**：侧边栏支持操作记录和市场问答

### 技术栈
- **语言**：Python 3.9+
- **框架**：PyQt6（GUI界面）
- **AI**：Gemini、Claude、DeepSeek 等大模型
- **数据源**：东方财富API、NGA论坛

---

## 📁 核心文件结构

```
e:\杂项\NGA狼\
├── main.py                 # 主程序入口，PyQt6界面
├── config.py               # 全局配置文件
├── positions.py            # 持仓数据管理
├── eastmoney_api.py        # 东方财富妙想API
├── eastmoney_client.py     # 东方财富行情客户端
├── llm_client.py           # 大模型API客户端
├── knowledge_base.py       # 知识库管理
├── smart_analysis.py       # 智能分析服务
├── daily_review.py         # 每日/每周复盘
├── chat_assistant.py       # 对话助手
├── task_scheduler.py       # 定时任务调度
├── intraday_analysis.py    # 盘中分析
├── ga_crawler.py           # NGA爬虫工具
│
├── knowledge_base/         # 知识库目录
│   ├── posts/             # 发言存储
│   │   ├── 202601/       # 按月份分类
│   │   └── ...
│   └── index.json         # 知识库索引
│
├── ngapost2md/            # NGA爬虫工具
│   └── ngapost2md.exe     # 可执行文件
│
├── positions.json          # 持仓数据
├── daily_operations.json  # 每日操作记录
├── daily_reports/         # 复盘报告存储
└── *.log                  # 日志文件
```

---

## 🔧 环境依赖

### Python 包
```
PyQt6>=6.4.0
requests>=2.28.0
pandas>=1.5.0
numpy>=1.23.0
```

### 外部工具
- `ngapost2md/ngapost2md.exe`：NGA帖子爬取工具

### API配置
- **东方财富妙想API**：获取市场行情数据
- **LLM API**：Gemini/Claude/DeepSeek 等大模型
- **NVIDIA API**：可选的高速推理API

---

## 🚀 转移步骤

### 步骤1：打包项目文件

需要转移的核心文件（排除知识库和缓存）：

```
# 核心代码
*.py                    # 所有Python源文件

# 配置文件
config.py               # 全局配置
positions.json          # 持仓数据（如需保留）
daily_operations.json   # 操作记录（如需保留）

# 知识库（可选，较大）
knowledge_base/         # 如需保留历史分析

# 外部工具
ngapost2md/             # NGA爬虫工具
```

### 步骤2：创建环境

在新环境中安装依赖：
```bash
pip install PyQt6 requests pandas numpy
```

### 步骤3：配置API

修改 `config.py` 中的API配置：
```python
# LLM API配置
LLM_API_URL = "你的LLM API地址"
LLM_API_KEY = "你的API Key"
LLM_MODEL = "模型名称"

# 东方财富API
MX_API_KEY = os.environ.get("MX_APIKEY", "你的Key")
```

---

## 📝 向 Trae Solo 介绍工程

当你启动新的 Trae Solo 时，可以使用以下提示词：

### 提示词模板1：简洁版
```
请帮我熟悉这个项目：e:\杂项\NGA狼

这是一个基于NGA狼大发言的智能投资分析助手，主要功能包括：
1. 增量爬取狼大最新发言
2. 知识库管理和智能分析
3. 持仓管理和盈亏计算
4. 每日/每周复盘报告
5. 盘中自动监测（每10分钟）

主要文件：
- main.py：PyQt6主界面
- positions.py：持仓管理
- daily_review.py：复盘报告
- knowledge_base.py：知识库
- config.py：全局配置

请先查看 config.py 和 main.py 了解项目结构。
```

### 提示词模板2：详细版
```
请帮我接手这个 NGA 狼大投资助手项目。

## 项目背景
这是一个 PyQt6 桌面应用，用于：
- 爬取 NGA 论坛狼大（ID:150058）的投资发言
- 使用大模型（Gemini/Claude）进行智能分析
- 管理个人股票持仓，计算盈亏
- 生成每日/每周复盘报告

## 核心功能
1. **盘中监测**：每10分钟爬取一次，自动分析新发言
2. **持仓管理**：支持自然语言记录买卖操作
3. **智能对话**：侧边栏支持市场问答
4. **定时报告**：每日23:59生成复盘报告

## 技术要点
- 使用东方财富API获取实时行情
- LLM分析采用分批处理，避免上下文过长
- 持仓数据保存在 positions.json
- 知识库存储在 knowledge_base/ 目录

## 当前状态
- ✅ 基础功能已完成
- ✅ LLM集成已配置
- ✅ 盘中监测已实现
- 🔄 复盘报告功能测试中

## 待优化
1. 分时数据获取失败时的容错处理
2. 知识库长期积累后的清理策略
3. 更多的市场数据源接入

请先阅读 main.py 和 config.py，了解整体架构。
```

---

## ⚠️ 注意事项

### 1. API Key 管理
- 不要将 API Key 直接提交到代码仓库
- 建议使用环境变量存储敏感信息
- 当前 config.py 中有硬编码的 Key，需要清理

### 2. 知识库大小
- `knowledge_base/` 目录可能很大（包含历史发言）
- 如果不需要完整历史，可以只转移近期数据
- 当前有约400+条发言记录

### 3. 路径问题
- 当前配置使用 Windows 绝对路径
- 转移后需要修改 `config.py` 中的 `BASE_DIR`

### 4. 外部依赖
- `ngapost2md.exe` 是编译好的工具，需要一起转移
- 或从原项目重新下载

---

## 📞 关键问题清单

在开始新会话前，请确认以下问题：

- [ ] API配置是否正确？
- [ ] 持仓数据是否完整？
- [ ] 知识库是否需要清理？
- [ ] 外部工具路径是否正确？
- [ ] 日志文件是否正常写入？

---

## 🔍 测试清单

转移后请测试以下功能：

1. ✅ 程序启动正常
2. ✅ 持仓数据显示正确
3. ✅ LLM调用成功
4. ✅ 知识库检索正常
5. ✅ 定时任务正常执行

---

如有任何问题，请参考项目的日志文件（`investment_analysis.log`）进行排查。
